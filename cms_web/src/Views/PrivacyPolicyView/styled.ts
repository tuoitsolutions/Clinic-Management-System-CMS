import styled from "styled-components";

export const StyledPrivacyPolicy = styled.div`
  letter-spacing: 0.3pt;
  word-spacing: 0.3pt;
  .policy-title {
  }

  p {
    padding: 0.5em 0;
  }

  ul {
    padding-left: 2em;
  }
`;
